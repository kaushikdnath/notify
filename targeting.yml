targets:

  ALL:
    description: "All active users"
    query: "(SELECT 'kaushik' as user_id) union (Select 'bok' as user_id) union (Select 'remmy' as user_id) union (Select 'vlad' as user_id) union (Select 'macdonald' as user_id) union (Select 'daryl' as user_id) union (Select 'wantei' as user_id) union (Select 'lamshai' as user_id)"
    param_source: null

  POLICE:
    description: "Users by Police group"
    query: "(Select 'macdonald' as user_id,'kordordonald@gmail.com'as email) union (Select 'daryl' as user_id,'daryl@gmail.com' as email) union (Select 'kaushik' as user_id,'kaushikdnath56@gmail.com' as email) union (Select 'daryl' as user_id,'daryl@gmail.com' as email)"
    param_source: null

  NIC:
    description: "Users by NIC group"
    query: "(SELECT 'kaushik' as user_id,'kdebnath56@gmail.com' as email) union (Select 'bok' as user_id,'leroymarbaniang@gmail.com' as email) union (Select 'vlad' as user_id,'evanstarmajaw27@gmail.com' as email)"
    param_source: null

  UNIT:
    description: "Users by unit id"
    query: "SELECT id as user_id FROM users WHERE unit_id = ?"
    param_source: "payload_json"
